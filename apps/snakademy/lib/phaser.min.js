/*! Phaser local compatibility placeholder for Snakademy.
   The official Phaser file could not be downloaded in this environment because DNS/network access is unavailable.
   Snakademy is implemented as a self-contained canvas game and runs offline. Replace this file with official Phaser 3 phaser.min.js if required. */
window.Phaser = window.Phaser || { VERSION: "local-compatibility-placeholder", Scale:{ FIT:"FIT", CENTER_BOTH:"CENTER_BOTH" } };
